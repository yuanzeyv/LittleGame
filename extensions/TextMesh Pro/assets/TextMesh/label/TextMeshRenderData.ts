import { IRenderData, math } from "cc";

export class TextMeshRenderData implements IRenderData {
    x: number;
    y: number;
    z: number;
    u: number;
    v: number;
    color: math.Color;

    
}