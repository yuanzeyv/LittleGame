import { _decorator, Component, Node, BlockInputEvents, Color, Sprite, Button } from 'cc'; 
import { BaseLayer } from '../../../Frame/BaseLayer/BaseLayer';
const { ccclass, property,type} = _decorator;
@ccclass('MainBoxLayer')
export class MainBoxLayer extends BaseLayer {  
    InitNode() {

    }
    InitData() {
    }
    InitLayer(){ 
    }
}


